package keeper

import (
	"context"
	"selfchain/x/selfvesting/types"
)

func (k Keeper) AddBeneficiary(goCtx context.Context) (*types.VestingInfo, error) {
	// TODO: Implement

	return &types.VestingInfo{}, nil
}
