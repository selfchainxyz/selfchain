syntax = "proto3";
package selfchain.identity;

option go_package = "selfchain/x/identity/types";

// CredentialIDs stores a list of credential IDs
message CredentialIDs {
  repeated string ids = 1;
}
